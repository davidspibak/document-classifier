[project]
name = "docclassify"
version = "0.1.0"
description = "Offline document auto-classification and multilingual semantic search"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
